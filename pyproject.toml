[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "deeplscalp"
version = "0.0.0"
description = "DeepLScalp research code"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["deeplscalp"]
